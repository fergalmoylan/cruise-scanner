name: Build UI

on:
  workflow_run:
    workflows: ["Scrape Cruise Prices"]
    types:
      - completed
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'web/**'
      - '.github/workflows/build_ui.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_PUSH }}
          persist-credentials: true

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24.7.0

      - name: Copy latest CSVs into published app
        run: |
          set -euo pipefail
          cp -f docs/cruise_prices_rcc.csv web/public/cruise_prices_rcc.csv
          cp -f docs/cruise_prices_msc.csv web/public/cruise_prices_msc.csv

      - name: Install deps & build
        working-directory: web
        run: |
          npm ci
          npm run build

      - name: Commit build + data to docs/app
        run: |
          set -euo pipefail
          git config user.name "GitHub Action"
          git config user.email "action@github.com"
          git add -A docs/app
          git add -f web/public/cruise_prices_rcc.csv
          git add -f web/public/cruise_prices_msc.csv
          if git diff --cached --quiet; then
            echo "No UI/data changes to commit."
            exit 0
          fi
          git commit -m "Build UI"
          git push origin HEAD:main
