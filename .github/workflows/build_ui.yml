name: Build UI

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'web/**'
      - 'docs/cruise_prices_v2.csv'
      - '.github/workflows/build-ui.yaml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_PUSH }}
          persist-credentials: true

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24.7.0

      - name: Copy latest CSV into published app
        run: |
          set -euo pipefail
          cp -f docs/cruise_prices_v2.csv web/public/cruise_prices_v2.csv

      - name: Install deps & build
        working-directory: web
        run: |
          npm ci
          npm run build

      - name: Commit build + data to docs/app
        run: |
          set -euo pipefail
          git config user.name "GitHub Action"
          git config user.email "action@github.com"
          git add -A docs/app
          git add -f web/public/cruise_prices_v2.csv
          if git diff --cached --quiet; then
            echo "No UI/data changes to commit."
            exit 0
          fi
          git commit -m "Build UI"
          git push origin HEAD:main
